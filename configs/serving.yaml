# Serving configuration - ports and runtime settings.

api:
  host: "0.0.0.0"
  port: 8000
  log_level: warning
  prediction_log_size: 1000  # rolling window kept in memory

mlflow:
  host: "0.0.0.0"
  port: 5001           # 5000 conflicts with macOS AirPlay Receiver

streamlit:
  host: "0.0.0.0"
  port: 8501
  headless: true
